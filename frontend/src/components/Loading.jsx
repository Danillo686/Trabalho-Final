/**
 * Loading.jsx — Componente de indicador de carregamento.
 *
 * Exibe uma mensagem simples de "Carregando..." enquanto
 * requisições assíncronas estão em andamento.
 */

export default function Loading() {
    return (
        <div className="loading">
            <p>Carregando...</p>
        </div>
    );
}
